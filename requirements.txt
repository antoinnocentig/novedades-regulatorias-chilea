# ── Dashboard Mercado Eléctrico Chileno — dependencias Python ──
# App / visualización
streamlit==1.40.2
plotly==5.24.1
pydeck==0.9.1
pandas==2.2.3
numpy==2.1.3

# Base de datos (SQLite por defecto, PostgreSQL opcional)
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10        # driver PostgreSQL (no se usa con SQLite)

# Extracción / scraping
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
pdfplumber==0.11.4             # extracción de tablas desde PDF (reportes CNE)

# Exportación
openpyxl==3.1.5                # Excel
XlsxWriter==3.2.0
reportlab==4.2.5              # PDF
kaleido==0.2.1                # exportar gráficos Plotly a PNG

# Utilidades
PyYAML==6.0.2
python-dotenv==1.0.1
tenacity==9.0.0               # reintentos con backoff
